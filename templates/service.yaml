apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp-color-helm.fullname" . }}
  labels:
    {{- include "webapp-color-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webapp-color-helm.selectorLabels" . | nindent 4 }}
